var util = require('util'),
  events = require('events');

function Router() {
  
  this.routes = {
    "get": {},
    "put": {},
    "post": {},
    "delete": {}
  };

  this.route = function (req, res) {
    
  };
}
